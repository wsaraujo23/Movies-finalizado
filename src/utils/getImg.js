

// o path e pra retorna a url  completa abaixo, sem passar o path dentro da fuctio ele nao retorna nada pois precisamos retorna o getImg com url
export function getImg(path) {



    return (
        `https://image.tmdb.org/t/p/original${path}`
    )
}

